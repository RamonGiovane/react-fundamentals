import React from "react"

// Componente funcional
export default function Primeiro(){
  const msg = 'Seja muito bem-vindo(a)'
  return (
  <div>
    <h2>Primeiro Componente</h2>
    <p>{ msg }</p>
  </div>
  )
}